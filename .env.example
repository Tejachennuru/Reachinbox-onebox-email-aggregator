# Server# Server Configuration

PORT=3000PORT=3000



# Email Accounts (JSON array)# Email Accounts (JSON array)

# Use a Gmail App Password or your provider's app-specific password# Example: [{"id":"acc1","email":"user@example.com","password":"password","imapHost":"imap.example.com","imapPort":993}]

EMAIL_ACCOUNTS=[EMAIL_ACCOUNTS=[{"id":"acc1","email":"tejachennuru05@gmail.com","password":"Teja123*","imapHost":"imap.example.com","imapPort":993}]

  {

    "id": "acc1",# Elasticsearch Configuration

    "email": "your-email@example.com",ELASTICSEARCH_NODE=http://localhost:9200

    "password": "your-app-password",ELASTICSEARCH_INDEX=emails

    "imapHost": "imap.gmail.com",

    "imapPort": 993# IMAP Configuration

  }IMAP_SYNC_DAYS=30

]IMAP_IDLE_TIMEOUT=1740000

IMAP_MAX_RECONNECT_ATTEMPTS=10

# AI (Gemini)IMAP_RECONNECT_DELAY=5000

AI_PROVIDER=gemini

AI_MODEL=gemini-2.5-flash# AI Configuration

AI_API_KEY=your_gemini_api_key_hereAI_PROVIDER=gemini

AI_API_KEY=AIzaSyBIGZ590W0m6GYvavVQrzO2IeGhUQ01yFQ

# ElasticsearchAI_MODEL=gemini-1.5-flash

ELASTICSEARCH_NODE=http://localhost:9200

ELASTICSEARCH_INDEX=emails# Webhook Configuration

WEBHOOK_INTERESTED_URLS=

# Webhooks (optional)WEBHOOK_SLACK_URL=

# Do NOT commit real URLs here. Keep these empty in example files.WEBHOOK_GENERIC_URL=

WEBHOOK_SLACK_URL=

WEBHOOK_GENERIC_URL=# Qdrant Configuration (Optional)

# Comma-separated list of additional Interested lead endpoints# QDRANT_URL=http://localhost:6333

WEBHOOK_INTERESTED_URLS=# QDRANT_API_KEY=

# QDRANT_COLLECTION=product_context

# App URL used in deep links (shown in Slack/webhooks)
APP_BASE_URL=http://localhost:3000

# IMAP settings (tuning)
IMAP_SYNC_DAYS=30
IMAP_IDLE_TIMEOUT=1740000
IMAP_MAX_RECONNECT_ATTEMPTS=10
IMAP_RECONNECT_DELAY=5000

# Qdrant (optional) for RAG
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION=product_context
